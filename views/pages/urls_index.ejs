<!-- views/pages/url_index.ejs -->

<!DOCTYPE html>
<html lang="en">
<head>
  <% include ../partials/_head %>
</head>
<body>

  <header>
    <% include ../partials/_header %>
  </header>

  <main style="font-size:20px; padding:75px;">
    <h1>URLS</h1>
    <ul style="margin:50px;">

      <% Object.keys(urls).forEach(function(URLId) { %>

      <li>
        <form method="POST" action=<%= `/urls/${URLId}/delete` %>>
          <label for="URLDetails"><%= URLId %>  -->  <%= urls[URLId] %>  <a href=<%= `/urls/${URLId}` %>>Edit</a></label>
          <button type="submit">Delete 🗑 </button>
        </form>
      </li>

      <% }); %>
    </ul>
  </main>

</body>
</html>